spring:
  application:
    name: config-service

management:
  endpoints:
    web:
      exposure:
        include: "*"
encrypt:
  keyStore:
    location: classpath:/server.jks
    password: letmein
    alias: ysptestkey
    secret: changeme