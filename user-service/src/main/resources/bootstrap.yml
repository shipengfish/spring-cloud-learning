server:
  port: 8000

spring:
  application:
    name: user-service
  cloud:
    config:
#      uri: http://localhost:8888
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: config-service
  profiles:
    active: dev

eureka:
  client:
    service-url:
      defaultZone: http://admin:admin@localhost:8761/eureka